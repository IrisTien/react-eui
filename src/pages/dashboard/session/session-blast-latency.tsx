import { FC } from 'react';
import React from 'react';
import { EuiPanel } from '@elastic/eui';

const DashboardSessionBlastLatency: FC = () => {
  return <EuiPanel>Latency(BLAST)</EuiPanel>;
};

export default DashboardSessionBlastLatency;
